pipeline {
  agent any
    stages {
      stage('welcome note') {
        steps {
         script {
           println "welcome to dvs devops"
           age = 30;
           println "my age is ${age}"
      }

      }
     
    }
  }
}
